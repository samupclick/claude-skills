# Families v1 — DRAFT, Sam to approve

Families are the fixed attribution keys (`families.name`). Variants are free text inside a family. Rename freely before batch one; after that, add via the Monday memo promotion path only.

## Format families (`kind = format`)

| name | one line | weekend template |
|------|----------|------------------|
| `job_photo_bubble` | real-looking work photo with a text bubble overlay, organic feel | yes |
| `screenshot_ad` | fake native UI: iMessage thread, notes app, email, calendar | yes |
| `ugly_ad` | plain text on a flat colour, no design, reads like a note | — |
| `founder_caption` | founder selfie or desk shot with a caption line | — |
| `tweet_card` | a tweet-style card with avatar, handle, and the hook as the post | — |
| `notification_mock` | phone lock-screen notification with the hook as the alert | — |
| `us_vs_them` | two-column comparison, old way vs new way | — |
| `checklist` | numbered or ticked list of 3–5 items | — |
| `stat_card` | one big number and one line of context | — |
| `whiteboard` | hand-drawn sketch or diagram photo | — |
| `press_quote` | quote card attributed to a publication or named source (release required) | — |
| `testimonial_card` | customer quote with attribution — **blocked** without `quote_release` and no depicted person | blocked |
| `before_after` | two-state comparison — policy-sensitive; no body or income claims | — |
| `meme` | recognisable meme format with our text | — |
| `carousel` | multi-frame story (organic only in phase 0) | — |

## Hook types (`kind = hook_type`)

`pain`, `outcome`, `proof`, `contrarian`, `identity`, `question`, `number`

## Angles (`kind = angle`)

`pain_led`, `outcome_led`, `proof_led`, `contrarian`, `identity`

## DTC seed list — PLACEHOLDER, Sam to confirm or replace

Brands widely known for high-volume creative testing in the Meta Ad Library. Chosen for format discovery, not category fit.

1. AG1 (Athletic Greens)
2. Liquid Death
3. HexClad
4. Ridge
5. Dr. Squatch

Category-peer list (fallback after a family is retired in-niche): to be filled by the intel worker's proposals, Sam picks 5.
