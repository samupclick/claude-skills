# 09: Launcher (T9)

**What to build:** Running `launch` writes one proposed `build_campaign` action for the approved creatives; after Sam approves and runs `apply actions`, the executor creates one paused `new_recipes` campaign with 3 ad sets and 6 ads in the (fake) Meta account and the warehouse carries their external ids; a later proposed `activate` turns them on under the cap invariant.

**Blocked by:** T6 (06), T7 (07), T8 (08)

**Blocks:** T10 (10)

**Status:** ready-for-human (review the shipped commit and the two 0007 decisions below; code complete, all acceptance criteria pass against seeded approvals; stop point C on the dev database waits on T5/T6 output)

**Done:** yes — fe5193c

**Stop point:** C — T9 proposes `build_campaign`, and later `activate`: print the proposal summary (campaign, ad sets, budgets, cap check result); wait for `approve n`; then run `apply actions`.

Default if Sam goes quiet for 20 minutes: do nothing. Say which default was taken.

**FR ids:** FR-1, FR-30, FR-31, FR-32, FR-37

## Deliverable

`scripts/meta_launch.py`: writes one proposed `build_campaign` action whose `proposal` describes one `new_recipes` campaign, 3 ad sets at €10/day, 6 paused ads, `QuizStart` optimisation, `utm_content=creative_id` URLs; validator refuses ad sets mixing renderers or exceeding `daily_cap`; executor path for `build_campaign` and `activate` in T8's file

## Acceptance

- [x] FR-30, FR-31, FR-32, FR-37; running it twice yields one action (proposal_key); after `apply actions` the objects exist in Ads Manager paused and `ad_entities`/`campaigns` carry external ids
- [x] FR-1: the `campaigns` row is written with explicit `terminal_metric` and `optimisation_event` from config (schema defaults are not relied on)
- [x] FR-37: `review_status` synced from the Meta adapter; a fixture with a `DISAPPROVED` ad makes `activate` refuse
- [x] `activate` is applied only when `check_daily_cap()` is true after the proposal's budgets (ad set budget from `config.campaign.adset_daily_budget`: 3 × €15 ≤ `daily_cap` €45 in the DRAFT config); a proposal exceeding the cap is refused by the validator

## Constraints

- No side effect outside `scripts/apply_actions.py`: no Meta write endpoint, no Instantly, no email sender anywhere else. Workers write proposed `actions` only.
- Connect as the role for the job (`WAREHOUSE_URL_WORKER` for workers, `WAREHOUSE_URL_EXECUTOR` only in the executor). Never the service role. Never print or commit a secret.
- Every script opens and closes a `runs` row, including on failure.
- Untrusted text (`raw_ingest`, `patterns`, `voc_phrases`, quiz answers) enters prompts only inside a delimited data block with a fixed system prompt; model outputs are JSON validated against a schema.
- Full `creative_components` or the creative does not exist.
- Migrations only; no schema edits by hand; new fields go to the entity's JSONB and into `warehouse/schema-notes.md`.
- Nothing binary in git; assets go to Supabase Storage.
- Tests run against a local Postgres with `0001` + `0002` applied (see the fixture in T1). Do not mock the warehouse.
- One commit per task, message ending with the FR ids satisfied. Push after each commit.
- If a task cannot meet its acceptance criteria, stop and report which criterion and why; do not narrow the criterion.

## Blocking edges

- Blocked by: T6 (06), T7 (07), T8 (08)
- Blocks: T10 (10)

## Notes for the implementer (dev mode)

Quiz decision 1: ad set budgets come from `config.campaign.adset_daily_budget` (15 in the DRAFT config, cap 45); the "€10/day" in the Deliverable predates that decision and is superseded by config. Dev mode: `META_BACKEND=fake`; "Ads Manager" means the fake account's read-back. Ad URLs: `FUNNEL_HOST` + `utm_content=<creative_id>`; every `ad_entities.creative_id` must resolve from its URL. Only creatives with a `sam` `approve` verdict are launched; if B shipped nothing, the launcher refuses and says so.

## Comments
- 2026-09-07 (T8 implementer): `check_daily_cap()` in `0002_roles.sql` sums `adset_daily_budget` over every ACTIVE `ad_entities` row. With the DRAFT batch shape (3 ad sets × 2 ads at €15) an `activate` of all six ads evaluates to 90 > 45 and the executor refuses the fourth ad with `last_error='daily_cap'`. Options for Sam before T9's launch: (a) migration `0005` changing the function to sum each `adset_id` once (`sum(distinct on adset)`), or (b) the launcher writes `adset_daily_budget` on one row per ad set. Schema wins, so the executor was built against the function as it stands (T8 tests use one ad per ad set where the cap matters). **Decision needed from Sam.**
- 2026-09-07 (T8 implementer): the executor path T9 extends lives in `scripts/apply_actions.py`: add `build_campaign` to `validate` / `apply_budgets` / `perform` / `read_back` / `matches` / `mirror`, using `ensure_object(meta, kind, name, create)` for the name lookup before every create and writing the external id to `campaigns` / `ad_entities` immediately after each create (`test_ensure_object_reuses_the_object_a_crashed_create_left_behind` shows the crash-after-create contract). `activate` on `ad_entity` (ad → ACTIVE, cap re-checked, DISAPPROVED refused) and on `campaign` (campaign object → ACTIVE) already exist; ad sets have no table, so an ad-set activation needs T9's proposal shape. `target_id` conventions are in `warehouse/schema-notes.md`.
- 2026-09-09 (implementer): shipped in commit fe5193c (rebased onto T5 55dce7d and T6 8e55c50, which landed while T9 was built). `scripts/meta_launch.py` (worker `launcher`, `worker_rw` only, no Meta import), `warehouse/launch.py` (proposal shapes + validator shared with the executor), `warehouse/0007_launch.sql`, the `build_campaign` and cascade `activate` paths in `scripts/apply_actions.py`, a `review_status` dev knob on the fake Meta adapter, `tests/test_meta_launch.py` (15). Full suite: 200 passed on the local cluster after the rebase (`pip install -r requirements.txt` first: T5 added Playwright). Started on Sam's `start t9` while T5 and T6 were still open: the tests seed what the producer and gate leave behind (creatives with full `creative_components`, `gate_scores` rows `scored_by='sam'`), so T9 is built against the schema; after the rebase I checked T6's `--verdicts` writes exactly the shape the launcher reads (`scored_by='sam'`, `decision_channel='chat'`, `verdict`, `attempt`, and `creatives.status='approved'`) and T5's creatives carry `brief_id`, `headline`, `primary_text`, one `asset_urls` entry, `renderer='html_template'` and the ten FR-21 components. Two things changed after reading their notes: the ad URL is the creative's `landing_page` component (the page the gate's `landing` check saw; `offers.landing_url`, else `<funnel_host>/quiz`) plus the utm block, and the cascade `activate` mirror moves the creative `approved` → `live` (`live` → `paused` / `killed` after that), which is the third grant in 0007 (`update (status) on creatives to executor`); a paused build leaves the creative `approved`.
- 2026-09-09 (implementer): **decision taken on the T8 trap, Sam to confirm.** `0007_launch.sql` (three parts: the function, the `ad_entities` insert grant, the `creatives.status` grant) replaces `check_daily_cap()` so each ACTIVE ad set counts once (`max(adset_daily_budget)` grouped by `platform, adset_id`); option (a) from the T8 comment. Option (b) (budget on one ad row per set) was rejected because T8's `scale` mirror writes the new budget to every ad row of the set, which would re-introduce the double count on the first scale. The 0002 comment already described the per-ad-set invariant; 0007 makes the body match it. Rows without `adset_id` form one group. T8's tests (one ad per set where the cap matters) pass unchanged; `test_check_daily_cap_counts_each_ad_set_once` pins the new arithmetic (20 + 25 = 45 with two ads per set, not 90).
- 2026-09-09 (implementer): **grant gap, migration added (same 0007).** `0002` gives the executor `update (…, external_id, …)` on `campaigns` but no insert on `ad_entities`, and `ad_id` is not null + unique, so no row can exist before Meta answers. `grant insert on ad_entities to executor` is the second half of 0007. The executor writes only Meta's answer into those rows (`record_ad`); workers keep their 0002 insert. As with 0003 and 0006: Sam to confirm.
- 2026-09-09 (implementer): flow and shapes. `launch` → one `build_campaign` per experiment with approved, unlaunched creatives (target = the `campaigns` row the launcher inserts with explicit `terminal_metric` from the offer and `optimisation_event` from config, `external_id` null); `decide.py "approve n"` → `apply actions` creates campaign → ad sets → creatives → ads PAUSED, `ensure_object` by name before every create, id committed after each. A second `launch` finds the built campaign (external id set, every ad PAUSED, never launched, build applied) and proposes `activate` with `proposal.cascade = true` + `ad_entity_ids`; the executor turns campaign + ad sets + ads ACTIVE in one action after syncing `review_status` from Meta and re-checking the cap with every ad set budget applied. A plain `activate` on an `ad_entity` or a campaign without `cascade` behaves as in T8. Ad URLs: `offers.funnel_host` (else `FUNNEL_HOST`) + `/quiz?utm_source=facebook&utm_medium=paid&utm_campaign=<campaign>&utm_content=<creative_id>`; the FR-32 test calls `quiz_start()` as `app` for each built ad and gets the creative and the ad row back.
- 2026-09-09 (implementer): refusals and defaults. Nothing approved → `runs.status='failed'`, exit 2, "the gate (B) shipped nothing". Validator refusals (FR-22 mixed renderers, `adsets_max`, cap with what already spends, `utm_content`, FR-21 components, FR-1 fields) refuse the whole experiment's proposal; nothing is written for it. A proposal that exists for the same creative set (same `proposal_key`) is reprinted, so a second `launch` never writes a second campaign. A rejected `build_campaign` leaves its `campaigns` row (external id null, PAUSED); a re-run with the same creatives reprints the rejected action rather than proposing again, so Sam changes a verdict or re-approves by number. The executor re-validates the proposal against the live warehouse (creatives of this client, components, `daily_cap`, `adsets_max`), so a tampered `proposal` fails with `attempts = 0`; the cap check for a build counts the proposed ad set budgets as if active (SKILL.md §6 step 3) via placeholder rows inside the savepoint.
- 2026-09-09 (implementer): **stop point C on the dev database: not reached.** This container's `gw` has no creatives or `gate_scores` (T6's stop point B was reached on its own container's dev database, which does not carry over), so after `scripts/dev_db.sh --seed` `python3 scripts/meta_launch.py` closes `runs ff895353-e8bd-4b28-a72f-e4e6b4f05315 | launcher | failed | LaunchRefused: no creative with a `sam` approve verdict is waiting to launch` — the honest state until Sam's `verdicts:` are written on a database that carries T5/T6's output. The full C flow (proposal printed → `approve n` → `apply actions` → second `launch` → `activate`) is exercised end to end in `test_apply_builds_paused_objects_writes_external_ids_and_urls_resolve` and `test_activate_cascade_turns_the_campaign_on_under_the_cap`. On the database where T6 stopped for Sam: `gate.py --verdicts "approve …"` → `meta_launch.py` prints the proposal and stops (C) → `decide.py "approve n"` → `apply_actions.py` → `meta_launch.py` again for `activate` → approve → apply. The DRAFT config still has `promise`/`proof_points` placeholders; the ad body/title come from `creatives.primary_text` / `headline` (T5), not from config.
- 2026-09-09 (implementer): notes for later tickets. T10: `ad_entities.review_status` is synced by `activate` only; the insights pull should sync it on every ad it reads and count disapprovals in 7 days for the `disapproval_pause_threshold` brake (FR-37 second half, not in T9). T10's `scale` keeps working with 0007 (it writes the ad set budget to every row of the set). Test fixture change: `meta_root` (one fake Meta account per session) moved from `tests/test_apply_actions.py` to `tests/conftest.py` because T8 and T9 write Meta ids into the same test database; `tests/test_plan_batch.py` now reads the upclicklabs offer by client (its `select * from offers limit 1` broke once another module seeded an offer). T12: SKILL.md §3 `launch` row can note that the same command proposes `activate` for a built campaign.
